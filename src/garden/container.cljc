(ns garden.container
  "Utility functions for working with container queries.")


(def condition-descriptors
  #{:aspect-ratio
    :block-size
    :height
    :min-height
    :max-height
    :inline-size
    :orientation
    :width
    :min-width
    :max-width})
